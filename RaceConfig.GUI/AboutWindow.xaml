<Window x:Class="RaceConfig.GUI.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About Archipelago Race Generator" SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding AppName}" FontSize="16" FontWeight="Bold"/>
        <TextBlock Grid.Row="1" Text="{Binding Version}" Margin="0,6,0,0"/>
        <TextBlock Grid.Row="2" Text="{Binding Copyright}" Margin="0,6,0,0"/>
        <TextBlock Grid.Row="3" Text="{Binding Description}" Margin="0,12,0,0" TextWrapping="Wrap" Width="420"/>
    </Grid>
</Window>