<UserControl x:Class="RaceConfig.GUI.View.GameOptions"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RaceConfig.GUI.View"
             xmlns:vm="clr-namespace:RaceConfig.GUI.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="740"
             Background="#D7DCF2">
             <!--Background="#F4EDD3">-->
            
    <UserControl.DataContext>
        <vm:GameOptionsViewModel/>
    </UserControl.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="500"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Game Options"
                       FontSize="20"
                       VerticalAlignment="Top"
                       Margin="10,0,0,0"/>
        
        <Grid Grid.Row="1" Margin="0,0,0,20">
            
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="400"/>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <!-- Path selector -->
            <TextBlock Text="Path:"
                       VerticalAlignment="Center"
                       Margin="10,0,8,0"
                       FontSize="16"/>
            <TextBox Grid.Column="1"
                     Height="25"
                     VerticalAlignment="Center"
                     VerticalContentAlignment="Center"
                     Text="{Binding TemplatesPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Grid.Column="2"
                    Content="Browse..."
                    Height="25"
                    Margin="8,0,0,0"
                    VerticalAlignment="Center"
                    Command="{Binding BrowseTemplatesPathCommand}"/>

            <!-- Template Selector -->
            <TextBlock Grid.Row="1"
                       Text="Template:"
                       VerticalAlignment="Center"
                       Margin="10,10,8,0"
                       FontSize="16"/>
            <ComboBox Grid.Row="1"
                      Grid.Column="1"
                      Grid.ColumnSpan="2"
                      HorizontalAlignment="Stretch"
                      Height="25"
                      Margin="0,10,0,0"
                      ItemsSource="{Binding TemplateFiles}"
                      DisplayMemberPath="DisplayName"
                      SelectedValuePath="FullPath"
                      SelectedValue="{Binding SelectedTemplateFile, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        </Grid>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Hidden">
            <ItemsControl ItemsSource="{Binding Options}" ItemTemplate="{StaticResource OptionRowTemplate}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

            </ItemsControl>
        </ScrollViewer>
        

        

    </Grid>
</UserControl>
