<Window x:Class="RaceConfig.GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:core="clr-namespace:RaceConfig.Core.Templates;assembly=RaceConfig.Core"
        xmlns:viewModel="clr-namespace:RaceConfig.GUI.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewModel:MainWindowViewModel}"
        mc:Ignorable="d"
        Height="720" Width="960"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True">



    <Border Background="#2E5266"
            CornerRadius="20">

        <Grid>
            
            
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Grid MouseLeftButtonDown="Grid_MouseLeftButtonDown"
                  Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Archipelago Race Generator"
                           Foreground="#E0E0E2"
                           FontWeight="Bold"
                           FontSize="25"
                           Grid.ColumnSpan="6"
                           VerticalAlignment="Center"
                           Margin="15,0,0,0"/>
                <Button Grid.Column="14"
                        x:Name="btnClose"
                        Content="X"
                        Width="40"
                        Height="40"
                        Background="Transparent"
                        Foreground="#E0E0E2"
                        FontSize="25"
                        BorderThickness="0"
                        Click="btnClose_Click"/>

            </Grid>


            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Row="1" Margin="0,10,0,0">
                    <RadioButton Content="Teams"
                             Height="50"
                             FontSize="20"
                             Style="{StaticResource MenuButtonTheme}"
                             Command="{Binding TeamsViewCommand}"/>

                    <RadioButton Content="Games"
                             Height="50"
                             FontSize="20"
                             IsChecked="True"
                             Style="{StaticResource MenuButtonTheme}"
                             Command="{Binding GameOptionsViewCommand}"/>

                    <RadioButton Content="Finalize"
                             Height="50"
                             FontSize="20"
                             Style="{StaticResource MenuButtonTheme}"
                             Command="{Binding FinalizeSettingsViewCommand}"/>
                </StackPanel>

                <ContentControl Grid.Column="1"
                                Margin="10"
                                Content="{Binding CurrentView}"/>
            </Grid>
        </Grid>

        

    </Border>

</Window>